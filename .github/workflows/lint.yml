name: Disabled lint workflow

on: []

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled workflow
        run: echo "This workflow is disabled. Use .github/workflows/ci.yml instead."
